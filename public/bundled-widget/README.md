# Secret Vault Reference Widget

This widget is installed automatically by the `siyuan-secret-vault` plugin.
Document blocks own their dormant presentation. The widget contacts the plugin
only after the user explicitly clicks **Connect**.
